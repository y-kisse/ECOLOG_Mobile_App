<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ECOLOG_Mobile_App.Views.MainPage"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             Title="{Binding Title}">

    <StackLayout HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand">
        <Label Text="ECOLOG Mobile App ver20180524" />
        <Button Text="Navi to DataInsertionPage" Command="{Binding NaviToDataInsertionPageCom}"/>
        <Button Text="Navi to EnergyStackPage" Command="{Binding NaviToEnergyStackPageCom}"/>
        <!--
        <Button Text="Navi to ECGsPage" IsEnabled="False" Command="{Binding NaviToECGsPageCom}" />
        <Button Text="Navi to ResultPage" IsEnabled="False" Command="{Binding NaviToResultPageCom}" />
        <Button Text="Navi to TestPlotPage" IsEnabled="False" Command="{Binding NaviToTestPlotPageCom}" />
        -->
    </StackLayout>

</ContentPage>
